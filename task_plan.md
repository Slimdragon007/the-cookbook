# Task Plan

> Architect-owned. Executors update status only. See base handbook Law 3.

## Active

- **TASK-018 — Paper Editorial reskin Phase 1 (foundation + canary surfaces).** Started 2026-05-20 on branch `feat/paper-editorial-foundation`. Replaces the Hearth theme (Magnolia/Playfair/Lora/gold `#C9A96E`) with Paper Editorial (paper `#F2EFE8` + card `#FCFBF7` + Instrument Serif italic + Inter + JetBrains Mono + terracotta `#D97757`) per the May 2026 Omelette prototype handoff. Backend invariant: nothing in `src/lib/`, `src/app/api/`, `src/middleware.ts`, Supabase schema, or env vars touched — pure visual swap. **Foundation:** `tailwind.config.ts` (new colors/fonts/glass-base), `src/app/layout.tsx` (next/font swap: Instrument_Serif + Inter + JetBrains_Mono; viewport themeColor `#D97757`; body `bg-paper text-ink`), `src/app/globals.css` (new `:root` vars + ambient/glass utilities). **Primitives retoned:** `Button` (primary now `bg-accent text-accent-on`, hover `bg-accent-ink`), `Input`/`InputLabel`, `MacroGrid` (cards now `bg-card border border-rule`), `MeasurementToggle`, `StepRibbon`. **No new primitives** (originally shipped `Surface`/`Tag`/`MacroPill` speculatively then deleted per YAGNI; the YAGNI cleanup was lost in #29's squash-merge and re-applied as commit `a39e95a` on TASK-023's branch). **Canary surfaces restyled:** home `/` (`(main)/page.tsx`, RecipeCard, RecipeGrid) — gallery with Instrument Serif italic headers + JetBrains Mono meta row + accent star ratings; `/recipe/[id]` (page + RecipeTabs + IngredientsTab + InstructionsTab + NutritionTab + RecipeActions) — display-italic titles, 4-cell stat row on rule grid, accent-soft step-number disc with serif-italic numeral. **Out of scope (deferred to follow-on tasks):** MainNav (TASK-019), `/log`+FoodLogForm+MealCard (TASK-020), `/summary`+WeeklySummary (TASK-021), `/grocery-list`+GroceryListBuilder (TASK-022), ChatDrawer+ChatFAB → "Kitchen line" (TASK-023), auth+demo surfaces (TASK-024), `/add-recipe`+`/profile` (TASK-025), Tweaks panel + multi-palette switcher (TASK-026, requires persistence design first). Verification: tsc clean, lint clean (`No ESLint warnings or errors`), vitest 164/171 (7 pre-existing skips), `npm run build` succeeds (all 11 routes generated, 3.66 kB on `/`, 7.29 kB on `/recipe/[id]`). E2E + browser smoke deferred to post-PR preview deploy (waiting on TASK-016 PR previews). `docs/design/julies-cookbook-design-bundle.md` updated with §3 Paper Editorial direction + Hearth deprecation banner. CLAUDE.md §2 styling row updated. Plan at `~/.claude/plans/the-cookbook-modular-moore.md`.

- **TASK-017 — Hearth reskin Phase 3 (Authenticated app), partial.** Two slices done 2026-04-30: Gallery (`feat/hearth-gallery` commit `298aa63`) and Food Log (`feat/hearth-food-log` commit `78ddb5e`), both rolled into a single PR off the post-PR-#22 main. **Slice A — Gallery:** RecipeCard reskinned per spec §4 (linen rounded card, 4:3/16:11 image, Playfair title, Lora meta, gold rating, switched div-onclick → button so keyboard nav works); RecipeGrid (Hearth search, Chip filter row per spec §2, Playfair "Your recipes" h2, 2-col mobile / 4-col desktop grid, two EmptyState variants per spec §17 with verbatim warm copy); home `/` page (drops gradient text for Playfair ink h1 + Lora subtitle, max-w-7xl wrapper, refreshed skeleton). **Slice B — Food Log:** MacroGrid extracted from NutritionTab to `src/components/ui/MacroGrid.tsx` as shared primitive (3rd use threshold hit: NutritionTab + Food Log + future Weekly), NutritionTab refactored to import it (no behavior change); FoodLogForm reskinned (bg-linen surface, Hearth Input/Button primitives, MealCard pattern for entries, EmptyState per spec §17); /food-log page header reskinned. All FoodLogForm logic preserved (calculateMacros, optimistic SWR mutation). **Remaining Phase 3 surfaces (separate work):** `/weekly` (BarChart + MacroGrid + Card), `/admin/import` (6 state cards), `/settings`. **Behaviorally deferred:** WeekStrip component, LogMealSheet bottom-sheet (drag-gesture lib needed). Lint/tsc clean; vitest 111/118 (7 pre-existing skips). Browser smoke deferred to post-merge production check.

## Backlog

- **TASK-019 — MainNav Paper Editorial reskin.** Done 2026-05-20 on branch `feat/paper-editorial-mainnav` (off `feat/paper-editorial-foundation`). `src/components/MainNav.tsx` rewritten end-to-end: page background `bg-paper`, brand badge now solid `bg-accent` (gradient dropped per handoff "no aggressive gradients"), Cookbook wordmark `font-display` (Instrument Serif italic) at 20-22px, nav-item active state `bg-card border border-rule shadow-lift-sm` with `text-accent-ink` label + `text-accent` icon, inactive `text-ink-soft hover:text-ink hover:bg-card/60`. Mobile top bar + bottom nav use `bg-paper/95 backdrop-blur-2xl border-rule`. Center Add tab uses `bg-accent` solid block (no gradient). Sidebar/mobile chat-entrypoint label renamed "Ask AI" → "Kitchen line" (handoff §"Chat renamed"); the panel internals (ChatDrawer header copy) remain TASK-023. Verification: lint clean, tsc clean, vitest 164/171 (7 pre-existing skips). E2E + browser smoke deferred to post-PR preview deploy.

- **TASK-020 — `/log` (food log) Paper Editorial reskin.** Done 2026-05-20 on branch `feat/paper-editorial-food-log` (stacked on `feat/paper-editorial-auth-demo`). Token swap across `src/app/(main)/log/page.tsx` (header typography Instrument-Serif-italic 40-52px, body `font-sans`) and `src/components/FoodLogForm.tsx` (form surface + log entry list items now `bg-card border border-rule shadow-lift-sm`; inline `selectClass` constant retoned `bg-card border-rule focus:border-accent`; `text-brown` labels and section headers → `text-accent`; entry name `font-display text-[18px]`; portion + calories columns swapped to `font-mono tabular-nums` per the handoff's mono-numerals discipline; empty-state icon disc `bg-accent-soft text-accent-ink`). All SWR mutation logic preserved (`useSWR('/api/log-meal?date=...')`, optimistic entry insertion, `calculateMacros` using `sumIngredientMacros`/`perServingMacros`/`calculatePortionMacros` from `src/lib/macros.ts`, `toGrams` portion conversion from `src/lib/unit-conversions.ts`). Verification: tsc clean, lint clean, vitest 164/171 (7 pre-existing skips). Browser smoke deferred to post-PR preview deploy.

- **TASK-021 — `/summary` (weekly summary) Paper Editorial reskin.** `src/app/(main)/summary/page.tsx` + `WeeklySummary.tsx`. Includes the weekly bar chart that the Pulse screen in the handoff describes. Verify the bar-chart height-resolution pattern from the prototype's section "Pulse bar chart" (explicit `height: 100%` columns + `justify-content: flex-end`). Effort: ~1.5 hours.

- **TASK-022 — `/grocery-list` (grocery) Paper Editorial reskin.** `src/app/(main)/grocery-list/page.tsx` + `GroceryListBuilder.tsx`. Aisle-grouped checklist with progress bar (matches handoff `MGrocery` screen). Effort: ~1 hour.

- **TASK-023 — Chat reskin + "Kitchen line" rename.** Done 2026-05-20 on branch `feat/paper-editorial-chat` (off `main`). Full rewrite of `src/components/ChatFAB.tsx` (34 LOC) and `src/components/ChatDrawer.tsx` (282 LOC), both of which had been pre-Hearth amber/slate (never reached by Phase 3). FAB badge: solid `bg-accent text-accent-on` with terracotta-tinted drop shadow (no gradient per handoff "no aggressive gradients"); `animate-bounce` pulse preserved as the chat's one signature interaction. Drawer header copy renamed "Ask Julie's Assistant" to "Kitchen line" (handoff §"Chat renamed"); aria-labels also updated. Modal backdrop `bg-ink/10 backdrop-blur-sm`, panel `bg-paper/95 backdrop-blur-2xl border-rule`. Quick-prompt buttons + retry button use accent-soft hover-accent pattern. User message bubble `bg-accent text-accent-on` (solid, no gradient); assistant bubble `bg-card border-rule text-ink`. Citation link `text-accent hover:text-accent-ink`. Submit button solid `bg-accent` with terracotta drop shadow. Input field uses local `bg-card border-rule focus:border-accent focus:ring-accent/15` pattern (not the `glass-input` class; consistency with rest of the reskin). Bullet color in `formatMessage` swaps `text-amber-500` to `text-accent`. Bold text in `renderInline` swaps `font-bold text-slate-800` to `font-semibold text-ink`. `data-chat-fab` and aria attributes preserved for e2e selectors. `/api/chat` endpoint untouched (backend invariant). Verification: tsc clean, lint clean, vitest 164/171 (7 pre-existing skips). PR also includes chore commit `a39e95a` removing dead `Surface`/`Tag`/`MacroPill` primitives that came back via #29's squash-merge.

- **TASK-024 — Auth + demo Paper Editorial reskin.** Done 2026-05-20 on branch `feat/paper-editorial-auth-demo` (off `feat/paper-editorial-mainnav`). Token + typography swap across `src/app/login/page.tsx`, `src/app/signup/page.tsx`, `src/app/auth/reset/page.tsx`, `src/app/auth/update-password/page.tsx`, `src/app/demo/page.tsx`. Method: bulk `sed` on the four auth pages (highly repetitive patterns), full Write on `demo/page.tsx` (per-step Hearth color semantics brown/ember/leaf/gold collapsed to the single accent ramp per "no aggressive gradients" handoff rule). Demo retained the StepRibbon ribbon + motion-react state machine verbatim — only the visual layer translated. Auth logic untouched (Supabase signInWithPassword, signup invite-code flow, resetPasswordForEmail, updateUser, PASSWORD_RECOVERY/SIGNED_IN event listener). Verification: tsc clean, lint clean, 0 dead Hearth tokens across the 5 files. Browser smoke deferred to post-PR preview (cloudflare-preview build was still running at commit time).

- **TASK-025 — `/add-recipe` + `/profile` Paper Editorial reskin.** `src/app/(main)/add-recipe/page.tsx` + `src/app/(main)/profile/page.tsx` + `AddRecipeForm.tsx` + `ProfileForm.tsx`. Effort: ~1 hour.

- **TASK-026 — Tweaks panel + multi-palette switcher.** From the Omelette prototype — runtime preferences for Voice (Editorial/Notebook/Studio), Imagery (Photographic/Filmic/Mono), Paper texture (Smooth/Linen/Newsprint), plus a 4-palette switcher (Paper/Bone-Sage/Ink/Terra wash). **Blocked on a persistence-layer ADR**: localStorage-only v1 vs. Supabase `user_preferences` table. Pre-ADR, sketch the settings-page UX (`/settings` doesn't exist yet). Probably 2-3 PRs once unblocked: ADR → schema/migration if Supabase path → UI. No effort estimate until persistence is decided.

- **TASK-016 — Cloudflare Pages preview deploys for PRs.** Surfaced 2026-04-30 during TASK-013 close-out. Current `.github/workflows/deploy.yml` only fires on `push: branches: [main]` and hardcodes `--branch=main`, so PR branches are invisible until merged. PR #20 (5 days of Hearth reskin work) and PR #21 (TASK-013 fix) both currently unreviewable except via local checkout. Fix shape: add `pull_request:` trigger, deploy with `--branch=${{ github.head_ref }}` so each branch gets a unique preview URL on `*.julies-cookbook.pages.dev`, comment the preview URL back on the PR via `actions/github-script` or similar. Caveats to think through in the ADR: secret exposure on PRs from forks (mitigated by `pull_request_target` or by restricting to same-repo branches), build cost (each PR push = a full next-on-pages build, ~3 min), and whether to clean up branch deploys when PRs close. Per Pitfall 6 + base handbook Law 4, this is an infra change → ADR required before workflow file is touched. Effort: ~30 min for the workflow change once ADR is accepted; ~1 hour for the ADR itself.
- **TASK-009 — Polish password reset UX.** Surfaced 2026-04-27 in post-merge review of PR #13. Two issues: (1) `update-password/page.tsx` has a race where `getSession()` can resolve before Supabase finishes parsing the URL hash, causing a brief "Link expired" flash before the form appears — fix by switching to `INITIAL_SESSION` event and dropping the `getSession()` fallback; (2) `createSupabaseBrowser()` is called on every render across `login`, `signup`, `auth/reset`, `auth/update-password` — memoize via `useState(() => createSupabaseBrowser())` for consistency. ~30 min total. Low priority but real.
- Populate `@docs/architecture/{ui,api,data}.md` with real content as each surface gets touched (currently stubs; `infra.md` is now real).
- Populate `@docs/REFERENCE.md` with full schema, env var contract, and file index from the live repo (currently stub).

## Done

- **TASK-015 — Hearth reskin Phase 2 (Recipe Detail).** Done 2026-04-30. Branch `feat/hearth-recipe-detail` off `feat/hearth-reskin`. Four commits across three logical units. **Commit 1 (`4a89875`) — Phase 2 prep:** ADR-005 (touch-target standard) accepted + implemented; `Button` primitive `icon` variant added; spec §6 ServingsScaler + plan callout updated; the Phase 2 architectural blocker resolved. **Commit 2 (`ed9fb93`) — tab contents:** IngredientsTab (ServingsScaler using Button icon variant per ADR-005, ingredient grouping by category, font-serif name + font-sans qty rows with linen-dim borders), InstructionsTab (numbered brown circles + Lora 16px text, restrained Bon Appetit completion card, dropped the connecting line and per-step glass cards), NutritionTab (unified MacroGrid subcomponent dropping per-macro coloring, portion calculator reskinned with linen→linen-dim gradient + cream input row, per-ingredient table with linen-dim borders + tabular-nums). **Commit 3 (`b23da51`) — TabBar:** RecipeTabs sticky top-0 (overrides spec's top-16; (main) MainNav is side/bottom nav), bg-glass-base + backdrop-blur, brown active underline, proper role="tablist"/role="tab"/aria-selected. **Commit 4 (`4499a6c`) — page assembly:** RecipeActions reskin (Button primitive for Edit/Replace photo/Delete, Input + InputLabel for edit form, rust for destructive cues), page.tsx full reskin (drops amber/orange decorative blobs, drops backdrop-blur-3xl frosted content panel for cream surface with linen-dim border, mobile back button uses Link with 44×44 hit area + 36px visual circle, Playfair display title, StatRow per spec §5 dropping the 4 colored icon cards for 4-cell linen-dim grid with no icons, Chip pattern for dietary tags, gold star rating, brown italic Lora source link). Plus IngredientsTab and RecipeTabs gain `lg:mx-0` to clamp the mobile bleed at desktop breakpoint. **Commit 5 (`fd2c2ae`) — pre-merge fixes:** Playfair + Lora actually loaded via next/font (was a critical bug — every font-display/font-serif class was silently falling back to Georgia); cn() now uses tailwind-merge for proper class deduplication; viewport.themeColor → Hearth gold #C9A96E; body className → text-ink bg-cream. Lint clean, tsc clean, vitest 111/118 (7 pre-existing skips). Browser smoke deferred to post-merge production check (no PR previews exist — TASK-016).
- **TASK-014 — Hearth reskin Phase 1 (public surface).** Done 2026-04-30. Architect-owned plan at `@docs/design/hearth-reskin-plan.md`. Phase 1 covers `/login`, `/signup`, `/auth/reset`, `/auth/update-password`, and `/demo` with the Magnolia + Liquid Glass token foundation laid in Phase 0 (commit `b0b9a91`). Sub-bullets: **1.1 `/login`** in commit `072babb`, introducing `Button` (primary/secondary/ghost) + `Input` + `InputLabel` primitives in `src/components/ui/` and `cn()` helper in `src/lib/utils.ts`. **1.2 `/signup` + 1.3 `/auth/reset` + `/auth/update-password`** in commit `8c5b4eb`; gold accent on signup invite-code per reskin-plan acceptance; `/auth/update-password` not in original Phase 1 scope but reskinned for consistency with the rest of the auth surface. **1.4 `/demo`** in this commit, with new `StepRibbon` component in `src/components/ui/StepRibbon.tsx`; demo motion-react state machine and 4-step animation preserved verbatim, only visual layer translated to Hearth tokens (brown/ember/leaf/gold accents per step, no amber/orange/emerald/pink). E2e selectors preserved across all reskinned surfaces (28/29 pass, 1 documented skip). All Phase 1 commits landed on PR #22 (PR #20 closed as superseded). Phase 2 (Recipe Detail) blocked on touch-target sizing decision: see TASK-013 cross-reference and the Phase 2 callout in the reskin plan.
- **TASK-013 — Mobile: ingredient +/- servings buttons not tappable; white space covers them.** Done 2026-04-30, merged via PR #21. Two-cause bug Julie surfaced 2026-04-29 on her phone. Cause 1 (layout): header `flex items-center justify-between` with no `flex-wrap` made the title pill (~232px wide) and the servings pill (~208px wide) collide on a 375px viewport — the right pill's `glass` backdrop blur visually "covered" the left, exactly her "white space covering the whole thing" symptom. Cause 2 (size): buttons were `w-9 h-9` (36px), below the 44px iOS HIG / WCAG 2.5.5 minimum. Fix in `src/components/IngredientsTab.tsx`: outer header now `flex-wrap gap-3` so the right pill drops to a new row on narrow viewports; each `<button>` is now `w-11 h-11` (44×44 hit area) wrapping a visual `w-9 h-9` `<span>` for the circle (visual unchanged, hit area expanded — Option 2 from architect decision); pill padding tightened `px-4` → `px-3`, inner `gap-3` → `gap-2` to compensate for the wider hit areas. Acceptance met on the layout side (header wraps cleanly on ≤375px) and the touch-target side (both buttons meet 44pt minimum). Architect-approved standard for the rest of the codebase encoded as ADR-005 + `Button variant="icon"` primitive (PR #22). When PR #22 merges, the Phase 2 IngredientsTab reskin replaces the inline pattern with the Button primitive — no regression. Lint clean, tsc clean, vitest unaffected.
- **TASK-010 — Scraper "pacing" error: bump deprecated Claude model + reset env example.** In PR #17, awaiting merge as of 2026-04-28. Slim reported the live `/add-recipe` flow returning a rate-limit-shaped "pacing" message after rotating the Anthropic API key. Investigation: production secrets all set on Cloudflare, audit endpoint green, no recent code regressions on the scrape path — but both `/api/scrape` (`extract.ts:174`) and `/api/chat` (`route.ts:76`) were pinned to `claude-sonnet-4-20250514`, the original Sonnet 4 from May 2025 (~11 months old, deprecation-window territory). Fix: bumped both call sites + `scripts/rescrape-all.mjs` to `claude-sonnet-4-6`. Bonus cleanup: rewrote stale `.env.local.example` (was still listing AIRTABLE keys from before the Mar 2026 Supabase migration). Lint/tsc/vitest clean (102/109, 7 pre-existing skips). Production audit re-checked post-PR — still `pass`. Local CLI smoke test blocked by parent `.env.local` missing the same keys the example fix calls out. See `@progress.md`.
- **TASK-011 / TASK-012 — Recipe images persist on scrape; targeted responsive cleanup.** Done 2026-04-28. After PR #17 fixed the Claude model string and scraping resumed, new recipes saved with `image_url = NULL`. Root cause at `src/lib/scraper/core.ts:215-224`: when `opts.cloudinary` was missing OR `uploadToCloudinary` returned null, `finalImageUrl` stayed `null` and the perfectly-good source URL was discarded before the DB write. Fix: default `finalImageUrl = imageUrl`, prefer Cloudinary URL when upload succeeds, `console.warn` + `ctx.errors` note on either failure path. Also widened `next.config.mjs` `remotePatterns` to `https://**` so source URLs render through `<Image>`. Added 3 unit tests covering the truth table (env null / upload 200 / upload 500). Responsive pass: `/recipe/*` pages now opt out of the `max-w-5xl` page wrapper (matches the full-bleed design intent), bottom-nav items switched from `min-w-[56px]` to `flex-1 min-w-0` (fits <320px phones), `<main>` got `min-w-0` defensively. Lint clean, tsc clean, 105/112 vitest pass (7 pre-existing skips). See `@progress.md`.
- **TASK-009 — Service worker no longer hides newly-added recipes from the gallery.** Done 2026-04-28. Surfaced when Julie added a honey-mustard recipe via URL, the API returned `success: true`, but the recipe never appeared in her gallery. DB audit confirmed the recipe was saved correctly under Julie's `user_id` with image + 9 ingredients — persist was innocent. Root cause: `public/sw.js` cached all HTML pages with stale-while-revalidate (returns cached first, fetches fresh in background) and **zero `caches.delete()` calls existed anywhere in the React app to bust the cached gallery after a successful save**. Fix: bumped `CACHE_NAME` from `cookbook-v2` → `cookbook-v3` (forces all clients to evict the stale cache on activate); switched HTML/page navigations from SWR to network pass-through; static assets and Cloudinary images keep their cache-first strategies (perf preserved). Added `router.refresh()` in `AddRecipeForm` after both URL- and text-submit success to evict the Next.js router cache as well. Lint clean, tsc clean. See `@progress.md`.
- **TASK-010b — Manual photo upload from recipe page.** Done 2026-04-27. Adds an escape hatch from the auto-scrape photo chain — when it produces nothing usable, the user replaces the photo with their own. Three pieces: (1) `uploadFileToCloudinary(file, publicId, env)` in `src/lib/scraper/cloudinary.ts` — POSTs Blob bytes via multipart, same signing protocol as the URL-based variant; (2) `POST /api/recipe/photo` (edge runtime) — auth check, ownership check via `recipes.user_id`, 15MB cap, JPEG/PNG/WebP/HEIC allowlist, uploads to Cloudinary at `${userId}/${slug}` namespace then updates `recipes.image_url`; (3) "Replace photo" button in `RecipeActions.tsx` (alongside Edit/Delete) triggers a hidden `<input type="file">` → uploads → `router.refresh()`. 3 new unit tests for the Cloudinary file helper. tsc fix: `File` global isn't in edge runtime types so we `instanceof Blob` (File extends Blob). Lint clean, tsc clean, vitest 105 pass / 7 skip. See `@progress.md`.
- **TASK-010a — ScrapingBee tier escalation.** Done 2026-04-27. `fetchScrapingBee()` now takes a `tier` param ("standard" = `render_js` only / ~5 credits; "premium" = adds `premium_proxy` / ~30 credits). New `fetchScrapingBeeWithEscalation()` helper tries standard first, escalates to premium on non-OK or thrown error. Wired into both SB invocation sites in `fetchPageWithFallback` (circuit-open path + 403 path). FetchResult gained an optional `scrapingBeeTier` field. 3 new unit tests (`src/lib/__tests__/scraper-parse.test.ts`) cover standard-wins / premium-after-standard-fails / both-fail. Lint clean, tsc clean, full vitest 102 pass / 7 skip. Effect: ~5x effective SB budget (most blocks resolve at cheap tier; premium reserved for hard sites). See `@progress.md`.
- **TASK-008 — Self-serve password reset.** Done 2026-04-27. Surfaced same day when Julie's login broke and admin-SQL was the only recovery path. Three changes: "Forgot?" link on `/login`, new `/auth/reset` (request email), new `/auth/update-password` (set new password — listens for `PASSWORD_RECOVERY`/`SIGNED_IN` events with a `getSession()` fallback, then `updateUser({ password })`). Middleware untouched (`/auth/*` already allowlisted). **Manual post-merge step:** add `https://julies-cookbook.pages.dev/auth/update-password` to Supabase Dashboard → Auth → URL Configuration → Redirect URLs (Dashboard-only, no MCP/Management API). Lint clean, tsc clean. See `@progress.md`.
- **TASK-008 — Recipes UNIQUE constraints + `.single()` error propagation (Issue #8, ADR-004).** Done 2026-04-27. ADR-004 accepted (Option A, Supabase CLI directory layout) — first repo-tracked migration. Discovered two pre-history migrations on the remote not tracked in repo + 4 `'preloaded'` sentinel rows that would block a naive constraint. Migration `20260427120000_recipe_uniqueness.sql` applied to production (`cqfszhxuvvsgusvjdyqx`) via Supabase MCP: normalized sentinel source_urls → NULL (4 rows), swapped global slug UNIQUE for per-user `(user_id, slug)`, added per-user `(user_id, source_url)` partial UNIQUE and `(user_id, name)` UNIQUE. Code: `persist.ts` writes NULL for `'manual entry'`; `data.ts` `getRecipeById` propagates non-PGRST116 errors. 3 new persist tests (102/109 vitest pass). Slug-collision retry logic deemed unnecessary — `(user_id, name)` UNIQUE makes within-user slug collisions impossible. Constraints verified live via `pg_indexes`. See `@progress.md`.
- **TASK-007 — Recipe detail tab redesign close-out (Notion Fix Queue, mockup approved 2026-03-10).** Done 2026-04-27. Audit found the structural refactor (RecipeTabs + IngredientsTab + InstructionsTab + NutritionTab) had already shipped in a prior session. Three remaining spec deltas closed: (a) body typography 15px → 16px (`text-base`) in IngredientsTab + InstructionsTab; (b) mobile hero `h-[50vh]` → `aspect-[4/3]` (desktop `lg:h-screen` preserved); (c) stats row 3-cell → 4-cell with per-serving Calories (computed server-side via `perServingMacros(sumIngredientMacros(...), servings)`, `Zap` icon, rose-50 swatch). Lint clean, tsc clean, 86/93 vitest pass (7 pre-existing skips). E2E not run (visual-only change, requires dev server). See `@progress.md`.
- **TASK-002 — Resolve dual scraper paths (ADR-002).** Done 2026-04-26 (Option B — TypeScript CLI via `tsx`). Shared scraper logic extracted to `src/lib/scraper/{contracts,normalize,fallback-table,macros,parse,cloudinary,extract,persist,core}.ts`. CLI migrated from `scripts/scrape-recipe.mjs` (964 lines, broken at parse time — pre-existing `await`-in-non-async-fn bug) to `scripts/scrape-recipe.ts` (~150 lines, runs via `tsx`). Web route shrunk from 1,113 → ~95 lines. 37 new unit tests landed (golden-master pure-function tests + happy-path orchestrator tests with mocked Supabase/Anthropic/fetch). Project CLAUDE.md: Rule 4 deleted, Pitfall 1 marked resolved. ADR-002 status: accepted + implemented. See `@progress.md`.
- **TASK-001 — Resolve deploy target (ADR-001).** Done 2026-04-25. Decision: Cloudflare Pages. `vercel.json` removed via `git rm`. Project CLAUDE.md and `@docs/architecture/infra.md` updated. Pitfall 6 marked resolved with institutional-memory note. See `@progress.md`.
- **TASK-004 — Remove dead Marketplace env-var fallback.** Done 2026-04-25. Refactored `src/lib/supabase/env.ts` (single naming scheme, kept lazy service-role accessor) and `src/app/api/audit/route.ts` (env check uses plain string list). Rule 5 rewritten in project CLAUDE.md. `@docs/REFERENCE.md` env section populated with the full runtime env-var list. Lint clean, tsc clean, 46/53 unit tests pass (7 pre-existing skips). E2E not run (needs dev server). Spawned TASK-005 for residual Vercel string cleanup found during the audit. See `@progress.md`.
- **TASK-005 — Residual Vercel string cleanup.** Done 2026-04-26. Three string/comment leftovers in `src/app/api/audit/route.ts` corrected: cron-secret comment de-Vercel'd, VERCEL_URL comment generalized to "runtime URL", Discord webhook footer updated to `julies-cookbook.pages.dev/api/audit`. Bundled per the original "next audit-route touch" guidance. Lint clean, tsc clean, 46/53 vitest pass. See `@progress.md`.
- **TASK-006 — Remove legacy `VERCEL` env var from Cloudflare Pages.** Done 2026-04-26. Deleted via `wrangler pages secret delete VERCEL --project-name=julies-cookbook`. Confirmed gone via `wrangler pages secret list`. `PEXELS_API_KEY` preserved (real code, not legacy). See `@progress.md`.
- **TASK-003 — Restore `/api/audit` cron under Cloudflare.** Done 2026-04-26. ADR-003 accepted (Option B). `.github/workflows/audit.yml` lands the daily 08:00 UTC schedule + `workflow_dispatch`. `AUDIT_SECRET` added as GitHub repo secret. `infra.md` Cron section rewritten. See `@progress.md`.
- **HUSKY — Pre-commit gate wired up.** Done 2026-04-26. `.husky/pre-commit` existed but was orphaned (husky not in package.json, hooksPath unset, hook not executable). Installed husky as devDependency, switched prepare script to modern `husky` invocation, hooksPath now `.husky/_`. Pre-commit (`next lint --quiet && tsc --noEmit`) confirmed firing on the install commit. Closes the gap between handbook §6 DoD and reality. Not previously tracked as a TASK because the handbook claimed this gate was already real. See `@progress.md`.
